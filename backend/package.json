{
  "name": "@syl/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Syl's Node service: the Claude Code harness, stores, jobs and HTTP/WebSocket surface.",
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.build.json && node scripts/copy-assets.mjs && node scripts/write-build-info.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "//onnxruntime-node": [
    "Pinned EXACTLY, and mirrored by an \"overrides\" entry in the root",
    "package.json which carries the full explanation. Short version: 1.24.0",
    "dropped darwin-x64 and this machine is an Intel iMac. The two must stay",
    "identical or npm fails with EOVERRIDE. Do not widen this to a caret range."
  ],
  "dependencies": {
    "@huggingface/transformers": "^4.2.0",
    "@syl/shared": "*",
    "express": "^5.2.1",
    "onnxruntime-node": "1.23.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/ws": "^8.5.13"
  }
}
