{
  "name": "syl",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Syl — a personal assistant harness driving Claude Code over its native stream-json stdio protocol.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "ping": "tsx src/cli/ping.ts",
    "when": "tsx src/cli/when.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
