// Root-level tooling only. Workspace source is typechecked by each
// workspace's own tsconfig.json — see `npm run typecheck`.
{
  "extends": "./tsconfig.base.json",
  "include": ["vitest.config.ts", "vitest.heavy.config.ts", "vitest.shared.ts"]
}
